[project]
name = "persistent-reasoning-light"
version = "0.1.0"
description = "Minimal deterministic prototype of the Persistent Reasoning Light engine."
authors = [
    { name = "Persistent Vlad" }
]
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["core"]